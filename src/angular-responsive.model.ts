export enum ResponsiveBreakpointName {
    custom = <any>'custom',
    mobile = <any>'mobile',
    tablet = <any>'tablet',
    desktop = <any>'desktop',
    large = <any>'large',
    extraLarge = <any>'extraLarge',
    infinite = <any>'infinite',
    zero = <any>'zero',
}

export let ResponsiveEvents = {
    resize: 'responsive:resize'
}
